[package]
name = "narrative-examples"
edition = "2021"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
narrative = { path = "../crates/narrative" }
futures = "0.3"
serde = { version = "1", features = ["derive"] }
